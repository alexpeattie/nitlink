engines:
  rubocop:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 18
  bundler-audit:
    enabled: true
ratings:
  paths:
  - lib/**/*
  - spec/**/*